
import {PAType} from './PAType';
import {User} from './user';

export class PARecord {
  cost: number;
  category: PAType;
  createdBy: User;
  createdDate: Date;
  name: String;
  group: String;
  }

